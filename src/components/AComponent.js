"use client";

import { StyledButton } from "@/app/index.styles";
import { NSButton } from "@newtonschool/grauity";

export const AComponent = () => {
    return (
        <>
            <NSButton>Oh hi</NSButton>
            <StyledButton>Click</StyledButton>
        </>
    );
};
